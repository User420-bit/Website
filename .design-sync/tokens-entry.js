/**
 * Leerer Einstiegspunkt fuer den Konverter.
 *
 * Klartext ist eine Astro-Seite: die Komponenten sind `.astro`-Dateien, die
 * beim Bauen zu HTML werden. Es gibt keine React-Komponenten, die sich in ein
 * Browser-Bundle packen liessen — deshalb synchronisiert dieses Repo nur die
 * Gestaltungsebene (Farben, Typografie, Raster, Bewegung, Schrift).
 *
 * Der Konverter braucht trotzdem einen Einstiegspunkt. Diese Datei exportiert
 * bewusst nichts: null Komponenten-Exporte schalten ihn in den
 * Tokens-only-Modus (`[ZERO_MATCH] ... treating as tokens-only DS`).
 */
export {}
